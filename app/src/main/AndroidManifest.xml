<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geekbrains.materialdesign">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FunnyTheme">
        <activity android:name="com.geekbrains.materialdesign.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.ApiActivity"/>
        <activity android:name=".ui.main.ApiBottomActivity"/>
        <activity android:name=".ui.main.AnimationsActivity"/>
        <activity android:name=".ui.main.AnimationaActivityBonus$AnimationsActivityBonus"/>
        <activity android:name=".ui.main.RecyclerActivity"/>
    </application>

</manifest>